"use client";

// Import the modularized visual template builder
import { VisualTemplateBuilder } from "./visual-builder";

// Export the modularized version as the default export
export default VisualTemplateBuilder;
